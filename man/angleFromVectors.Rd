% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/angleFromVectors.R
\name{angleFromVectors}
\alias{angleFromVectors}
\title{Angles Between Vectors and Reference Vectors}
\usage{
angleFromVectors(v, w, normalized = FALSE, deg = TRUE)
}
\arguments{
\item{v}{Numeric vector or matrix. If a matrix, each column represents one
vector.}

\item{w}{Numeric vector or matrix used as reference. If a vector, it is used
as a common reference for all columns of \code{v}; if a matrix, it must be
compatible with \code{v} for column-wise angle computation.}

\item{normalized}{Logical. If \code{TRUE}, vectors are assumed to be already
normalized. If \code{FALSE}, internal normalization is performed.}

\item{deg}{Logical. If \code{TRUE}, angles are returned in degrees; otherwise in
radians.}
}
\value{
A numeric vector containing the computed angles.
}
\description{
Computes angles between the columns of \code{v} and one or more reference vectors.
}
\details{
If \code{w} is a vector, it is used as a single reference vector for all columns
of \code{v}. If \code{w} is a matrix, angles are computed column by column between
\code{v} and \code{w}.

If \code{normalized = FALSE}, vectors are internally normalized before computing
the angles. If \code{normalized = TRUE}, all vectors are assumed to be unit
vectors, i.e., vectors with Euclidean norm equal to 1.

To avoid numerical errors in \code{acos()}, cosine values are clamped to the
interval \eqn{[-1, 1]}.
}
\examples{
v <- matrix(c(1, 0, 0,
              0, 1, 0,
              0, 0, 1), nrow = 3)

w <- c(1, 0, 0)

angleFromVectors(v, w, normalized = FALSE, deg = TRUE)

}
