% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binningMarchingCubes.R
\name{binningMarchingCubes}
\alias{binningMarchingCubes}
\title{Generate a Triangular Mesh Using Binning and Marching Cubes}
\usage{
binningMarchingCubes(
  panel,
  resol_nbin = c(4, 4, 2),
  mesh_cad = NULL,
  zero_layers_z = 1,
  k_factor = NULL,
  trunc_factor = 3.5,
  level_factor = 0.99,
  cad_coverage_tol = 0.99
)
}
\arguments{
\item{panel}{A numeric matrix or data frame with three columns containing the X, Y
and Z coordinates of the point cloud.}

\item{resol_nbin}{A numeric vector of length three defining the approximate binning
resolution in the X, Y and Z directions. Default is \code{c(4, 4, 2)}.}

\item{mesh_cad}{Optional. A \code{mesh3d} object used as a reference to select or merge
connected mesh components. Default is \code{NULL}.}

\item{zero_layers_z}{Integer. Number of zero-valued layers added above the binning
grid in the Z direction. Default is \code{1}.}

\item{k_factor}{Numeric. Factor used to compute the tolerance for removing
low-density nodes. If \code{NULL}, it is automatically assigned according to
\code{resol_nbin}. Default is \code{NULL}.}

\item{trunc_factor}{Numeric. Factor used to truncate high-density node weights.
Default is \code{3.5}.}

\item{level_factor}{Numeric. Factor used to define the isosurface extraction level
from the truncated weights. Default is \code{0.99}.}

\item{cad_coverage_tol}{Numeric. Minimum proportion of the CAD model dimensions
that must be covered by the reconstructed mesh when \code{mesh_cad} is provided.
Default is \code{0.99}.}
}
\value{
A list containing the reconstructed \code{mesh3d} object, the processed binning
object, the triangular surfaces returned by \code{misc3d::contour3d()} and the parameters
used during the reconstruction process.
}
\description{
Reconstructs a triangular surface from a 3D point cloud and returns it as a
\code{mesh3d} object. The function performs linear binning using \code{npsp::binning()},
adds external zero-valued layers in the Z direction, removes low-density nodes,
truncates high-density nodes and extracts the isosurface using
\code{misc3d::contour3d()}.
}
\details{
If a CAD mesh is provided, disconnected mesh components are sorted by size and
merged until the reconstructed mesh reaches the specified proportion of the CAD
model dimensions.
}
\examples{
\dontrun{
result <- binningMarchingCubes(
  panel = panel,
  resol_nbin = c(4, 4, 2),
  mesh_cad = mesh_cad,
  zero_layers_z = 1
)

mesh <- result$mesh

rgl::open3d()
rgl::shade3d(mesh, col = "lightblue")
rgl::axes3d()
}

}
\seealso{
\code{npsp::binning()}, \code{npsp::coordvalues()}, \code{misc3d::contour3d()},
\code{rgl::tmesh3d()}
}
