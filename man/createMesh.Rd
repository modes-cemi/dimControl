% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createMesh.R
\name{createMesh}
\alias{createMesh}
\title{Reconstruct a Triangular Mesh from a 3D Point Cloud}
\usage{
createMesh(
  panel,
  resolNbin = c(4, 4, 2),
  meshCad = NULL,
  zeroBorder = FALSE,
  kFactor = NULL,
  truncFactor = 3.5,
  levelFactor = 0.99,
  coverageTol = 0.99
)
}
\arguments{
\item{panel}{A numeric matrix or data frame with three columns containing the X,
Y, and Z coordinates of the point cloud.}

\item{resolNbin}{A numeric vector of length three defining the approximate binning
resolution in the X, Y, and Z directions. Default is \code{c(4, 4, 2)}.}

\item{meshCad}{Optional \code{mesh3d} object used as a reference for the spatial coverage
of the reconstructed surface. Default is \code{NULL}.}

\item{zeroBorder}{Logical. If \code{TRUE}, adds a zero-valued layer around the X and Z
limits before surface extraction. Default is \code{FALSE}.}

\item{kFactor}{Numeric factor used to determine the tolerance for removing low-density
nodes. If \code{NULL}, it is selected automatically. Default is \code{NULL}.}

\item{truncFactor}{Numeric factor used to truncate high-density node weights. Default
is \code{3.5}.}

\item{levelFactor}{Numeric factor used to define the isosurface extraction level.
Default is \code{0.99}.}

\item{coverageTol}{Numeric value between 0 and 1 defining the minimum spatial coverage
required relative to the reference bounding box. Default is \code{0.99}.}
}
\value{
A list containing:
\itemize{
\item \code{mesh}: reconstructed surface as a \code{mesh3d} object.
\item \code{bin}: processed binning object.
\item \code{contours}: disconnected surfaces generated by Marching Cubes, ordered from
largest to smallest.
\item \code{params}: parameters used during reconstruction.
}
}
\description{
Reconstructs a triangular surface from a 3D point cloud using linear binning and
Marching Cubes, and returns the result as a \code{mesh3d} object.
}
\details{
Linear binning is performed with \code{\link[npsp:binning]{npsp::binning()}} and the grid coordinates are
obtained with \code{\link[npsp:coordvalues]{npsp::coordvalues()}}. Low-density nodes are removed and high-density
weights are truncated before extracting the isosurface with \code{\link[misc3d:contour3d]{misc3d::contour3d()}}.

When \code{zeroBorder = TRUE}, one zero-valued layer is added on both sides of the X
and Z directions to facilitate surface closure.

If several disconnected surfaces are produced, they are ordered by size and progressively
merged until the reconstructed surface reaches \code{coverageTol} times the dimensions
of the reference bounding box.
}
\examples{
\dontrun{
# Load the theoretical CAD model
data("cad", package = "dimControl")

# Prepare the theoretical panel mesh
meshTeor <- preparePanelMesh(cad)

# Simulate panel points without floor points
panel <- simulatePanelFloorCloud(
  mesh = meshTeor,
  nPanel = 1e7,
  nFloor = 0,
  seed = 1
)

# Reconstruct the mesh from the panel point cloud
result <- createMesh(
  panel = panel,
  resolNbin = c(4, 4, 2),
  zeroBorder = TRUE
)

mesh <- result$mesh

# Display point cloud and reconstructed mesh side by side
rgl::clear3d()
rgl::mfrow3d(1, 2)

rgl::points3d(panel, col = "blue")
rgl::title3d("Panel point cloud", level = 4)

rgl::next3d()
rgl::shade3d(mesh, col = "lightgray")
rgl::title3d("Reconstructed mesh", level = 4)
}

}
\seealso{
\code{\link[npsp:binning]{npsp::binning()}}, \code{\link[npsp:coordvalues]{npsp::coordvalues()}}, \code{\link[misc3d:contour3d]{misc3d::contour3d()}}, \code{\link[rgl:mesh3d]{rgl::tmesh3d()}}
}
